export enum Role {
    SUPER_ADMIN = 'super_admin',
    ADMIN = 'admin',
    CUSTOMER = 'customer',
}

export enum MediaFileAssociations {
    NEWS = 'news',
}

export enum MediaFileUsages {
    DOCUMENT = 'document',
}

export enum MediaFileStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export enum Status {
    PENDING = 'pending',
    CONTACTED = 'contacted',
}

export enum ScheduledDayStatus {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
}

export enum Order {
    ASC = 'ASC',
    DESC = 'DESC',
}

export enum RoleEnum {
    ADMIN = 'admin',
    USER = 'user',
}

export enum UserType {
    PUPIL = 'pupil',
    TEACHER = 'teacher',
}
